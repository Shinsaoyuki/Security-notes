# nmap オプション完全リファレンス

> PenTest+ (PT0-003) 対策・実務向けチートシート

---

## 目次

1. [ターゲット指定](#ターゲット指定)
2. [スキャンタイプ](#スキャンタイプ)
3. [ポート指定](#ポート指定)
4. [サービス・OS検出](#サービスos検出)
5. [NSEスクリプト](#nseスクリプト)
6. [タイミング](#タイミング)
7. [ファイアウォール回避・スプーフィング](#ファイアウォール回避スプーフィング)
8. [出力形式](#出力形式)
9. [その他便利オプション](#その他便利オプション)
10. [頻出コマンド例](#頻出コマンド例)

---

## ターゲット指定

| オプション | 説明 |
|-----------|------|
| `192.168.1.1` | 単一IPを指定 |
| `192.168.1.1-254` | IP範囲を指定 |
| `192.168.1.0/24` | CIDR表記 |
| `-iL targets.txt` | ファイルからターゲット読み込み |
| `--exclude 192.168.1.5` | 特定IPを除外 |

---

## スキャンタイプ

| オプション | 説明 | root権限 |
|-----------|------|----------|
| `-sS` | **SYN スキャン**（ステルス）。SYNを送りSYN/ACKが返ったらRSTで切断。最も一般的 | 必要 |
| `-sT` | **TCP Connect スキャン**。3ウェイハンドシェイク完了。ログに残りやすい | 不要 |
| `-sU` | **UDP スキャン**。DNSやSNMPなどの検出に使用。遅い | 必要 |
| `-sN` | **Null スキャン**。フラグなし。Windowsには効かない | 必要 |
| `-sF` | **FIN スキャン**。FINフラグのみ送信 | 必要 |
| `-sX` | **Xmas スキャン**。FIN+PSH+URGを送信 | 必要 |
| `-sA` | **ACK スキャン**。ファイアウォールのルール確認に使用 | 必要 |
| `-sI <zombie>` | **Idle スキャン**。第三者ホストを踏み台にした完全ステルス | 必要 |
| `-sn` | **Ping スキャン**（旧`-sP`）。ポートスキャンなし、ホスト発見のみ | — |

---

## ポート指定

| オプション | 説明 |
|-----------|------|
| `-p 80` | ポート80のみ |
| `-p 80,443,8080` | 複数ポート |
| `-p 1-1024` | 範囲指定 |
| `-p-` | **全ポート**（1〜65535） |
| `-F` | **Fast**（上位100ポートのみ） |
| `--top-ports 1000` | 上位1000ポート |
| `-p U:53,T:80` | UDPとTCPを混在指定 |

---

## サービス・OS検出

| オプション | 説明 |
|-----------|------|
| `-sV` | **サービスバージョン**検出 |
| `--version-intensity 0-9` | バージョン検出の強度（デフォルト7） |
| `-O` | **OS検出** |
| `--osscan-guess` | OS推測を積極的に行う |
| `-A` | **積極スキャン**（`-O -sV --traceroute -sC` 相当） |

---

## NSEスクリプト

| オプション | 説明 |
|-----------|------|
| `-sC` | **デフォルトスクリプト**実行（`--script=default` と同等） |
| `--script=<name>` | 特定スクリプト実行（例：`--script=http-title`） |
| `--script=vuln` | 脆弱性スクリプト一括実行 |
| `--script=smb-vuln-*` | SMB脆弱性チェック |
| `--script-args` | スクリプトへの引数渡し |

### スクリプトカテゴリ

| カテゴリ | 用途 |
|---------|------|
| `auth` | 認証関連 |
| `brute` | ブルートフォース |
| `discovery` | サービス探索 |
| `exploit` | 脆弱性の悪用 |
| `vuln` | 脆弱性検出 |

---

## タイミング

| オプション | 別名 | 説明 |
|-----------|------|------|
| `-T0` | paranoid | 非常に遅い（IDS回避） |
| `-T1` | sneaky | 遅い（IDS回避） |
| `-T2` | polite | 遅め（帯域を抑える） |
| `-T3` | normal | **デフォルト** |
| `-T4` | aggressive | 速い（高速ネットワーク向け） |
| `-T5` | insane | 最速（精度低下あり） |

---

## ファイアウォール回避・スプーフィング

| オプション | 説明 |
|-----------|------|
| `-f` | **パケット断片化**（フィルタ回避） |
| `--mtu <size>` | MTU手動指定（8の倍数） |
| `-D RND:10` | **デコイスキャン**（偽IPを混在させる） |
| `-D <ip1,ip2>` | 特定デコイIPを指定 |
| `-S <IP>` | 送信元IPを偽装 |
| `-e <iface>` | 使用インターフェース指定 |
| `--source-port 53` | 送信元ポートを偽装（DNS等に偽装） |
| `--data-length <n>` | ランダムデータをパケットに付加 |
| `--scan-delay <ms>` | パケット間の遅延を設定 |
| `--badsum` | 不正チェックサムで送信（IDS検出に） |

---

## 出力形式

| オプション | 説明 |
|-----------|------|
| `-oN <file>` | **Normal** 形式で保存 |
| `-oX <file>` | **XML** 形式で保存 |
| `-oG <file>` | **Grepable** 形式で保存 |
| `-oA <basename>` | **全形式**で同時保存（.nmap / .xml / .gnmap） |
| `-oS <file>` | **Script kiddie** 形式で保存。文字を leet speak（1337speak）に変換した出力。`a→@`、`e→3`、`i→!`、`o→0` など。実用目的はなく、ユーモア的機能 |
| `-v` | 詳細表示（`-vv` でさらに詳細） |
| `-d` | デバッグ出力 |
| `--open` | open ポートのみ表示 |
| `--reason` | ポート状態の理由を表示 |

### `-oS` の出力イメージ

通常の `-oN`：

```
PORT   STATE SERVICE
22/tcp open  ssh
80/tcp open  http
```

`-oS`（leet speak変換後）：

```
P0RT   $TATE $ERVIC3
22/tcp 0p3n  $sh
80/tcp 0p3n  http
```

> **補足**：実務・試験での使用頻度はほぼゼロ。nmapのイースターエッグ的な機能。

---

## その他便利オプション

| オプション | 説明 |
|-----------|------|
| `-n` | **DNS解決を無効化**（高速化） |
| `-R` | 常にDNS解決を実施 |
| `--dns-servers <IP>` | 使用するDNSサーバ指定 |
| `-Pn` | **Ping無効化**（ホストがUp前提でスキャン） |
| `-PS` | TCP SYN Ping |
| `-PA` | TCP ACK Ping |
| `-PU` | UDP Ping |
| `--traceroute` | トレースルートを実行 |
| `--iflist` | 利用可能なインターフェース一覧 |
| `--resume <file>` | 中断したスキャンを再開 |

---

## 頻出コマンド例

### ステルスSYNスキャン + バージョン検出 + デフォルトスクリプト

```bash
sudo nmap -sS -sV -sC -p- -T4 -oA result 192.168.1.0/24
```

### UDPスキャン（上位100ポート）

```bash
sudo nmap -sU --top-ports 100 192.168.1.1
```

### OS検出 + 積極スキャン

```bash
sudo nmap -A -T4 192.168.1.1
```

### ファイアウォール回避（断片化 + デコイ）

```bash
sudo nmap -sS -f -D RND:5 192.168.1.1
```

### 脆弱性スクリプト実行

```bash
sudo nmap --script=vuln 192.168.1.1
```

### SMB脆弱性チェック（EternalBlue等）

```bash
sudo nmap --script=smb-vuln-* -p 445 192.168.1.1
```

### Ping無効 + 全ポート + 結果保存

```bash
sudo nmap -Pn -p- -T4 -oA fullscan 192.168.1.1
```

---

## 参考

- [nmap 公式ドキュメント](https://nmap.org/book/man.html)
- [NSE スクリプトライブラリ](https://nmap.org/nsedoc/)
- CompTIA PenTest+ PT0-003 exam objectives

---

*Last updated: 2026-04-10*
